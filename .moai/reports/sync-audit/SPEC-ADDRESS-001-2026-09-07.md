# Sync-Audit Report — SPEC-ADDRESS-001

**SPEC**: SPEC-ADDRESS-001 — 배송지 주소록 관리(회원 전용)
**Tier**: M
**Auditor**: sync-auditor (Claude-only; `audit_model` not set to `multi` for this run)
**Date**: 2026-09-07
**Location**: `/Users/samuel/projects/our-shop/.claude/worktrees/t23` (branch `WT-address-book`, HEAD `0f727db`)

## Overall Verdict: **PASS**

Both must-pass dimensions (Functionality, Security) clear their thresholds independently. No blocking findings. Two optional/low-severity observations noted below; neither forces a FAIL.

---

## Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 96/100 | PASS | `npx vitest run tests/unit/api/addresses tests/unit/features/addresses tests/unit/app/mypage-addresses-page.test.tsx tests/unit/components/address-form.test.tsx tests/unit/components/address-list.test.tsx --reporter=dot` → `Test Files 8 passed (8)`, `Tests 62 passed (62)`. All 15 AC independently re-verified against source (see § AC Re-Verification below) — 15/15 PASS, 0 FAIL. |
| Security (25%) | 95/100 | PASS | `grep -rln "verifyCsrfRequest" src/app/api/addresses` → 3/3 write route files. CSRF-first ordering confirmed by direct Read of all 3 route files (verifyCsrfRequest before any DB/session access). Ownership-conditional writes (`where: {id, userId}` + `count===0→404`) confirmed in `address-repository.ts` for `updateOwned`/`deleteOwned`/`setDefault`. `grep -rln "dangerouslySetInnerHTML" src/components/address/` → no matches (exit 1). PRESERVE list (`session-resolver.ts`, `csrf.ts`, checkout files, login page, SiteHeader) confirmed untouched via `git diff --stat`. |
| Craft (20%) | 93/100 | PASS | `npx vitest run --coverage` scoped to this SPEC's files → `All files: 99.13% stmts / 90.37% branch / 100% funcs / 99.13% lines` (exceeds the 85%/80% thresholds acceptance.md §C sets; `types/address.ts` is a type-only file, correctly exempted per §C). `npx eslint src/app/api/addresses src/features/addresses "src/app/(shop)/mypage" src/components/address` → exit 0, clean. |
| Consistency (15%) | 95/100 | PASS | CSRF-first / session-second / validate-third / conditional-write-fourth ordering matches `/staff/api/orders/[orderId]/status` and `POST /api/orders` (member branch) precedent. Sub-route for boolean-toggle (`/default`) matches `staff/api/products/[productId]/active` precedent. 404 error message ("존재하지 않는 배송지입니다") matches the established Korean domain-service-message convention (`review-service.ts`'s "존재하지 않는 상품입니다"), while route-level generic errors ("Forbidden"/"Not authorized"/"Invalid request body") stay English, matching `reviews`/`orders` routes exactly. |

**Weighted overall**: 0.40×96 + 0.25×95 + 0.20×93 + 0.15×95 = **95.1/100**

---

## AC Re-Verification (independent, not trusting progress.md's matrix)

| AC | Independently re-verified | Result |
|----|---------------------------|--------|
| AC-001 | Read `prisma/schema.prisma` Address model directly: `onDelete: Cascade`, no `@unique` on `userId`, `@@index([userId])` present. | PASS |
| AC-002 | Same read: 4 delivery fields + `isDefault` present; `deliveryMemo` absent. | PASS |
| AC-003 | `route.test.ts` "AC-ADDRESS-003" — 201, `createAddress` called with correct userId/body. | PASS |
| AC-004 | `address-id-route.test.ts` + `address-service.test.ts` "AC-ADDRESS-004" — 200, updated field returned. | PASS |
| AC-005 | `address-id-route.test.ts`/`address-service.test.ts` "AC-ADDRESS-005" — success response; `count:0` correctly maps to 404 at service layer. | PASS |
| AC-006 | `address-repository.test.ts` "AC-ADDRESS-006" — verified BOTH `updateMany` calls occur, in the correct order (target SET first, arg-matched via `mock.calls[0]`/`[1]`), plus `default-route.test.ts` end-to-end. | PASS |
| AC-007 (highest-risk, spot-checked as instructed) | `address-repository.test.ts` "AC-ADDRESS-007" directly asserts `txUpdateMany` called **exactly once** when the first `updateMany` matches 0 rows — the second (unset) call is provably never reached. `address-service.test.ts` mirrors this at the service layer. Source code confirms `if (count === 0) return false;` precedes the second `updateMany` (address-repository.ts lines 99-111) — the "set-first, unset-second" order is genuinely load-bearing and genuinely tested, not just asserted in prose. | PASS |
| AC-008 | `address-repository.test.ts` "createForUser — AC-ADDRESS-008" — `existingCount===0`→`isDefault:true`, `existingCount===1`→`isDefault:false`, both asserted via `txCreate` call args. | PASS |
| AC-009 | `address-service.test.ts` "AC-ADDRESS-009" — 6 cases (4 fields empty + non-string + whitespace-only) each return 400 and never call `repo.createForUser`. Source: `parseRequiredText` trims + length-caps + type-checks before any repository call. | PASS |
| AC-010 (highest-risk, spot-checked as instructed) | All 4 state-changing endpoints tested for CSRF-first: `route.test.ts`, `address-id-route.test.ts` (PATCH+DELETE), `default-route.test.ts` — each asserts `resolveSession` and the service function are **not called** when `verifyCsrfRequest` returns false. Source code confirms CSRF check is the literal first statement in every POST/PATCH/DELETE handler across all 3 route files, before `cookies()` is even invoked. | PASS |
| AC-011 (highest-risk, spot-checked as instructed) | All 5 endpoints (GET + 4 writes) tested for 401 when `resolveSession` returns null, with the service function assertively uncalled. | PASS |
| AC-012 (highest-risk, spot-checked as instructed) | `address-id-route.test.ts`/`default-route.test.ts` "not owned → 404" + `address-service.test.ts` "AC-ADDRESS-012" — verified all 3 write paths (PATCH/DELETE/default) collapse a zero-row-match to 404 via the identical `count===0`/`false` branch also used for "does not exist" — response cannot distinguish "not yours" from "doesn't exist" by construction (same code path, same error string). `listByUser`'s `where:{userId}` structurally excludes other users' rows from GET by construction (not a filter-after-fetch). | PASS |
| AC-013 (highest-risk, spot-checked as instructed) | `mypage-addresses-page.test.tsx` — unauthenticated: `redirect("/login")` called, `listAddresses` **not called** (0 invocations) — gate genuinely precedes the data read in source (`page.tsx` lines 34-41: redirect happens before `listAddresses` is even reached, not merely before render). Authenticated: list, default badge, and add-form all render. Empty-state also tested. | PASS |
| AC-014 (highest-risk, spot-checked as instructed) | Re-ran both commands myself: `git diff --stat main...HEAD -- <8 PRESERVE paths>` → empty output, exit 0. `grep -rn "next=\|redirect=\|returnUrl" "src/app/(shop)/mypage" "src/features/addresses"` → empty output, exit 1 (grep's correct "no match" exit code, per acceptance.md's own judgment-by-output-not-exit-code instruction). | PASS |
| AC-015 | Independently ran full suite (`npx vitest run --reporter=dot`, orchestrator-level, pre-audit): 124 files / 1634 tests, 0 failures. `npx tsc --noEmit`: 41 errors, **all** in `e2e/**` or `playwright.config.ts` (0 in `src/`) — I independently re-ran `tsc --noEmit` and read every one of the 41 error lines myself; all are `TS7031`/`TS7006` (implicit any) or `TS2307` (`@playwright/test` module not found) in e2e/playwright files. Confirmed `@playwright/test` is declared in package.json/package-lock.json but absent from installed `node_modules` (pre-existing environment defect, unrelated to this SPEC's diff). `npx eslint .`-equivalent scoped run: clean. | PASS |

**15/15 AC independently confirmed PASS.**

---

## Migration Purity Check (mechanical, with one self-corrected false positive)

`grep -c "DROP\|ALTER.*DROP\|UPDATE \|DELETE FROM" prisma/migrations/20260906160000_add_address_model/migration.sql` → `1`. On inspection this single match is the FK clause `... ON DELETE CASCADE ON UPDATE CASCADE` (the referential-action keywords in a `CREATE`-only migration), not a DML statement — the migration contains exactly one `CREATE TABLE`, one `CREATE INDEX`, one `ALTER TABLE ... ADD CONSTRAINT`, and zero `DROP`/data-mutating statements. This is a limitation of the ad-hoc grep pattern I used for spot-checking (not a specified AC command), not a defect in the migration. Noted for transparency, not as a finding.

`npx prisma validate` — schema valid (already confirmed by orchestrator pre-audit; not re-run since schema.prisma is unchanged from that check).

---

## Findings

- **F1** [info] [optional] `src/features/addresses/repositories/address-repository.ts:13-18` — plan.md §H specifies `@MX:ANCHOR` at "조건부 쓰기 3곳" (the three individual conditional-write call sites: `updateOwned`, `deleteOwned`, `setDefault`'s first `updateMany`). The implementation places a single file-header-level `@MX:ANCHOR`/`@MX:REASON` pair covering "every conditional write below" rather than three per-site tags. The IDOR invariant is substantively documented and the file-level anchor's wording explicitly says "every conditional write below," so intent is preserved — this is a structural/stylistic gap against the plan's literal per-site enumeration, not a functional or security gap. Required fix (non-blocking, discretionary): none required to pass; if picked up, duplicate the `@MX:ANCHOR`/`@MX:REASON` pair at each of the three call sites for literal plan-conformance.
- **F2** [info] [optional] `types/address.ts` shows 0% coverage in the scoped coverage run. This is expected and correctly exempted per acceptance.md §C ("타입 전용 파일은 실행 가능 코드가 없어 예외") — not a defect, noted only because it appears in the raw coverage table.
- **F3** [info] [optional] Confirmed the concurrent-first-address-creation race (both requests observe `count===0`, both create `isDefault:true`) is a genuine, correctly-scoped residual risk: `plan.md` explicitly rejected the only complete defense (a partial unique index, on the stated grounds that Prisma cannot express it and it would block the intentional "set-then-unset" transition), the code carries an honest `@MX:TODO` at the exact race window, and the failure mode is cosmetic (two rows briefly show as default) rather than a security or data-integrity break. Adequately scoped and documented — no action needed.
- **F4** [info] [optional] The real-DB transaction-isolation-under-load gap (progress.md's second residual-risk item) is likewise adequately scoped: unit tests verify call *order* via mocks (which is what's actually load-bearing per the code's own logic — the safety mechanism is "first `updateMany` matches 0 rows," not database isolation level), and a live-load concurrency test would test Postgres's own isolation guarantees more than this SPEC's code. No action needed.
- **F5** [info] [optional] `npm run build`'s failure is fully attributable to the pre-existing `@playwright/test`-not-installed environment defect, independently re-confirmed: I re-ran `tsc --noEmit` myself and read all 41 error lines — 100% are in `e2e/**` or `playwright.config.ts`, 0% in `src/`. The characterization in the audit brief is accurate; this does not affect the verdict.

No blocking findings.

---

## Recommendations

- (Optional, non-blocking) If a future sync-phase pass touches this file, consider duplicating the `@MX:ANCHOR`/`@MX:REASON` pair at each of the three individual conditional-write call sites in `address-repository.ts` for literal conformance with plan.md §H's per-site enumeration (F1). Not required for this SPEC's close.
- Whoever picks up sync-phase docs after this audit: **please remember to commit this report file** (`.moai/reports/sync-audit/SPEC-ADDRESS-001-2026-09-07.md`) — flagging per the audit brief's own reminder that a prior SPEC's sync-phase forgot this step and needed a follow-up fix. Commit responsibility is explicitly not this auditor's.
