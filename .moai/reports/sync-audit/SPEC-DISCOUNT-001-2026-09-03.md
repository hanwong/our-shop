# Sync-Audit Report — SPEC-DISCOUNT-001

Date: 2026-09-03
Auditor: sync-auditor (independent post-implementation quality audit, --deep lens)
Worktree at audit time: `.claude/worktrees/t7`, branch `WT-coupon-discount-engine`, HEAD `fb2f19d820a95da35b89610d4f343e58199c7fc0`

## Evaluation Report
SPEC: SPEC-DISCOUNT-001
Overall Verdict: FAIL

Must-pass firewall: Functionality PASS, Security PASS (neither dimension independently fails its threshold). The FAIL verdict is driven by a **blocking finding** (F1) per the finding-consumption discipline — a real, mechanically-reproduced CI-breaking defect, not by the must-pass firewall.

### Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 85/100 | PASS | 24/25 AC independently verified by direct source-code reading + targeted test runs (see § AC Verification below). All money-path arithmetic, transaction ordering, and PRESERVE targets confirmed correct. Deduction for the operational consequence of F1 (SPEC's own new tests will not execute correctly in the actual CI target). |
| Security (25%) | 94/100 | PASS | No injection risk (Prisma parameterized queries throughout); all money-determining arithmetic is server-authoritative (`confirmedTotal` is a cross-check only, never trusted — `order-service.ts` 3e); `payment-service.ts:83` amount gate independently confirmed unchanged (`git diff e5b5537..HEAD -- src/features/payments/services/payment-service.ts` → 0 lines, re-run by this auditor); coupon-enumeration risk on `POST /api/discounts/validate` is transparently disclosed as an accepted, out-of-scope risk in design.md §5 / research.md §5 (not a hidden vulnerability); input validation present (`route.ts` `parseBody` rejects non-string/non-integer payloads with 400). |
| Craft (20%) | 60/100 | FAIL | `npx eslint .` → exit 0 (this auditor, this run). `npx tsc --noEmit` → exit 0 (this auditor, this run). Project-wide coverage (this auditor, this run, DB reachable): `npx vitest run --coverage --exclude "**/tests/integration/auth/login.test.ts"` → `All files \| 97.71 \| 93.83 \| 100 \| 97.71` (well above the 85/85/80/85 vitest.config.ts thresholds) — BUT this healthy number is reachable only because a live Postgres happened to be reachable in this audit environment; see F1/F2 below for why the CI-representative picture is materially different. |
| Consistency (15%) | 70/100 | PASS | Repository conventions followed closely: `Coupon`/`DiscountType` schema mirrors the `Category`/`Order` snapshot-not-FK precedent; `incrementRedeemedCountIfAvailable`/`decrementRedeemedCountIfPositive` mirror `order-repository.ts`'s `updateMany`-conditional-atomic shape exactly; `DiscountFailure` mirrors `OrderFailureCode`'s 409-everything shape; module boundaries (`features/discounts/{types,services,repositories}`) mirror `features/orders/`. Deduction: F1 is a direct violation of the one live-DB-test-gating convention this exact SPEC's own sibling file (`concurrency.postgres.test.ts`, SPEC-ORDER-002 precedent) demonstrates the author was aware of. |

### Focus-Area Findings (per the orchestrator's 5 directed questions)

**1. Plan-audit PASS-with-debt disposition — defensible, but not independently verifiable.** The described trajectory (iter1 FAIL 0.79 → iter2 FAIL 0.90 → iter3 FAIL 0.95, PASS-with-debt after the 3-iteration cap) is internally consistent with the documented plan-auditor contract (a must-pass/blocking defect forces FAIL regardless of score margin), and the sole iter-3 remaining defect (one stale self-referential sentence in research.md) is cosmetic/textual, not a design or requirements gap — a reasonable thing to accept at the retry ceiling. **However**, I could not independently confirm this: `ls -la .moai/reports/plan-audit/` in this worktree shows only `.gitkeep` — the three review report files (`SPEC-DISCOUNT-001-review-{1,2,3}.md`) genuinely do not exist here (re-verified this run). progress.md §E.1's account is the *only* source for the D1-D4/N1-N2 finding detail and the score trajectory; nothing in this worktree lets me cross-check it against a primary artifact. progress.md is honest about this gap (already flagged in §E.3 as a sync-phase carryover item), so this is not a fabricated-PASS claim — but it remains an unverified claim, and I flag it as residual risk (F4) per the same discipline that caught a prior SPEC's unverified plan-audit PASS claim.

**2. AC-DISCOUNT-016 capability gate + observed outcome — verified correct.** Read `tests/integration/orders/concurrency.postgres.test.ts` directly: the gate is a genuine reachability probe (`await prisma.$queryRaw\`select 1\`` at module load, `reachable`/`skipReason` captured, every live-DB `describe` wrapped in `describe.skipIf(!reachable)`) — not a rubber-stamped `true`. This auditor independently confirmed the DB is reachable in this exact environment (`nc -z -v -G 2 localhost 5433` → succeeded; `npx prisma migrate status` → "Database schema is up to date!" against `localhost:5433`). The coupon-race test body (lines 493-577) asserts exactly what progress.md M4 claims: both promises settle (no unhandled error), exactly one succeeds, the loser gets 409/`COUPON_EXHAUSTED`, `redeemedCount` ends at exactly 1, and exactly one `Order` row is created carrying the coupon snapshot. progress.md's disclosure that this AC is `SKIPPED` (not `PASS`) in the actual CI environment (no `services: postgres` in `.github/workflows/ci.yml`, confirmed by this auditor reading the workflow file directly) is accurate and matches the CI-workflow's own placeholder `DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/..."`. No discrepancy found between claim and code.

**3. M4-M5 atomic redeem/release symmetry — verified correct.** `incrementRedeemedCountIfAvailable(tx, ...)` is called at `order-service.ts:557`, inside the SAME `tx` that the order-creation `$transaction` callback receives (the function that ALSO performs the stock-decrement loop and the order insert). `decrementRedeemedCountIfPositive(tx, ...)` is called at `payment-repository.ts:181`, inside `markOrderCancelledAndRestoreStock(tx, orderId)` — and that function is called ONLY from `payment-service.ts:273`, itself inside `prisma.$transaction(async (tx) => { ... })` (verified by reading `payment-service.ts:262-289` directly), which ALSO performs the stock-restore loop in the same callback. Both increment and decrement are `updateMany`-conditional-atomic (never plain read-then-write), symmetric in shape (`redeemedCount: { lt: maxRedemptions }` / `{ gt: 0 }`), and both execute in the same DB transaction as their corresponding stock effect. The atomicity claim holds structurally, confirmed by direct source reading rather than by trusting progress.md's prose.

**4. Money-adjacent correctness — verified correct, including the floor-rounding commitment.** `order-service.ts` step ordering (read directly, lines ~490-572) is exactly 3a (itemsSubtotal) → 3b (coupon validate) → 3d (`totalAmount = itemsSubtotal - discountAmount + shippingFee`, `shippingFee` untouched per REQ-DISCOUNT-005) → 3e (`confirmedTotal` cross-check against the DISCOUNTED total, `PRICE_CHANGED` on mismatch) → 3f (atomic increment, `COUPON_EXHAUSTED` on race loss) → step 4 (stock decrement loop) — matching design.md §3.1 verbatim. `payment-service.ts:83`'s `if (order.totalAmount !== amount)` reads the already-discounted `Order.totalAmount` and is unchanged (0-line diff, re-verified this run) — no mismatch is created because the discount is baked in before this comparison ever runs, and the comment at that line correctly attributes the check to REQ-PAYMENT-006 (the confirm-redirect path), matching spec.md's D4 correction. `calculateDiscount` (`discount-engine.ts:32-43`) implements `Math.floor((itemsSubtotal * coupon.value) / 100)` for `PERCENTAGE` — genuine floor, not round or ceil — and its passing unit test (`tests/unit/discounts/discount-engine.test.ts`, part of the 7/7 pass observed this run) exercises the `33333 × 10% → 3333` boundary AC-DISCOUNT-007 specifies. No rounding bug found.

**5. Two already-resolved sync-phase items — not re-flagged.** The plan-audit report-file absence is addressed above as a residual-risk (F4), consistent with "gitignored local artifact, not necessarily an anomaly" per progress.md's own framing — not raised as a new defect. The `t22` backlog-card claim for coupon-usage-release-on-abandonment was not independently checked (no access to the kanban queue from this worktree) and is taken at face value per the delegation instructions.

### Findings (structured defect-list)

- **F1** [High] [blocking] `tests/integration/discounts/coupon-model.test.ts:1-46`, `tests/integration/discounts/validate-write-free.test.ts:1-37` — Both files open a REAL Postgres connection unconditionally (no `describe.skipIf(!reachable)` capability gate), unlike the established precedent in `tests/integration/orders/concurrency.postgres.test.ts` (the ONE other file in this repo that touches a real DB, correctly gated). Both files' own doc comments explicitly (and incorrectly) reason that "no capability gate is needed here... this milestone's pre-flight already established reachability as a precondition" — conflating the local dev machine's reachability with CI's. `.github/workflows/ci.yml` sets `DATABASE_URL: "postgresql://ci:ci@127.0.0.1:5432/our_shop_ci?schema=public"` with no `services: postgres` block, and its own inline comment states as an existing repo invariant: "Nothing in CI opens a database connection: the test suite mocks the Prisma seam". **Reproduced this run**: `DATABASE_URL="postgresql://ci:ci@127.0.0.1:5432/our_shop_ci?schema=public" npx vitest run --coverage ...` → `FAIL tests/integration/discounts/coupon-model.test.ts` and `FAIL tests/integration/discounts/validate-write-free.test.ts`, both with `PrismaClientInitializationError: Can't reach database server at 127.0.0.1:5432`, 2 failed suites / 4 failed tests. This will fail the required `verify` GitHub Actions check (`Test with coverage` step, `npm run test:coverage`) on the next push/PR to `main` that includes this branch. **Required fix**: wrap both files' `describe(...)` blocks in the same `reachable`/`skipReason` probe pattern `concurrency.postgres.test.ts` already uses (`await prisma.$queryRaw\`select 1\`` at module load, `describe.skipIf(!reachable)(...)`), or fold their assertions into an already-gated file.

- **F2** [Medium] [blocking] `src/features/discounts/repositories/coupon-repository.ts:76-86` (`incrementRedeemedCountIfAvailable`) — the single function enforcing REQ-DISCOUNT-016's over-redemption guard has NO fast/deterministic unit test verifying its `updateMany` where/data shape with a mocked `tx` client, unlike its sibling `decrementRedeemedCountIfPositive` (which HAS exactly such a test, `tests/unit/discounts/coupon-repository.test.ts:94-105`) and unlike `order-repository.ts`'s equivalent `decrementStockIfAvailable` (100% covered by a dedicated fake-client unit test, confirmed this run: `npx vitest run --coverage ... tests/unit/orders/order-repository.test.ts` → `100 \| 100 \| 100 \| 100`). **Reproduced this run**: `npx vitest run --coverage ... tests/unit/discounts tests/integration/discounts tests/unit/orders tests/integration/orders/order-service.test.ts` (i.e. every fast/mocked test that could exercise this function, excluding the live-DB concurrency test) → `coupon-repository.ts \| 60.71 \| 100 \| 66.66 \| 60.71 \| 76-86` — the increment function's body is entirely unexecuted by any fast/mocked test. Combined with F1, this means: in the actual CI environment, this safety-critical query shape is verified by **zero** tests (the live-DB concurrency test that is its only real-execution coverage today always skips in CI). **Required fix**: add a fake-tx-client unit test asserting the exact `where: { id: couponId, redeemedCount: { lt: maxRedemptions } }` / `data: { redeemedCount: { increment: 1 } } }` call, mirroring the existing `decrementRedeemedCountIfPositive` test block.

- **F3** [Low] [optional] `src/features/discounts/services/discount-engine.ts:22` — the `@MX:ANCHOR` comment states `calculateDiscount` is "shared by the checkout screen and order-service" — but `CheckoutInteractive.tsx` never calls `calculateDiscount` directly; it fetches an already-computed `discountAmount` from `POST /api/discounts/validate`, which calls it transitively via `validateCoupon`. Confirmed by grep: `calculateDiscount(` has exactly one real call site (`discount-service.ts:75`); no match in `src/components/checkout/` or `src/app/checkout/`. The single-source-of-truth property design.md §2 describes IS preserved (transitively, through the API boundary), so this is a documentation-precision issue, not a functional defect. **Suggested fix (optional)**: reword to "shared by every caller of the discount-service layer (the validate endpoint and the order transaction)".

- **F4** [Low] [optional, residual-risk] `progress.md` §E.1's plan-audit trajectory (0.79 → 0.90 → 0.95, PASS-with-debt) rests entirely on progress.md's own narrative — the underlying report files are genuinely absent in this worktree (verified: `ls -la .moai/reports/plan-audit/` → only `.gitkeep`). progress.md is honest about this (flagged in §E.3), so this is not a false-PASS claim, but it is an unverifiable claim from this auditor's vantage point. No specific inconsistency was found in the narrative itself (the trajectory is internally consistent with the documented plan-auditor must-pass-forces-FAIL contract), so this is recorded as residual risk rather than as a defect.

### Recommendations

- Fix F1 before this branch integrates: add the capability gate to both new live-DB integration test files. This is the one change that actually blocks safe merge — everything else in this SPEC is in good shape.
- Fix F2 alongside F1 (small, low-cost addition; the pattern to copy already exists in the same file).
- F3/F4 are informational; no action required before merge, though F3 is a one-line comment fix if convenient.
- Once F1/F2 are addressed, re-run `DATABASE_URL="postgresql://ci:ci@127.0.0.1:5432/our_shop_ci?schema=public" npm run test:coverage` locally (or equivalent) to confirm the CI-representative run is actually green before pushing — this is the verification step that was missing from the run-phase evidence chain (every verification in progress.md was run against the reachable dev-machine DB, never against a CI-representative unreachable one).
