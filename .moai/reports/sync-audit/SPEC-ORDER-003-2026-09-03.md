# Sync-Phase Quality Audit — SPEC-ORDER-003

**SPEC**: SPEC-ORDER-003 — 게스트 주문 재방문 조회와 주문 상태 표시
**Tier**: M
**Branch**: feat/SPEC-ORDER-003
**HEAD audited**: a92c978
**Diff range audited**: `284a492..HEAD` (7 commits, M1–M4)
**Auditor**: sync-auditor (independent, skeptical evaluation — not a rerun of the orchestrator's self-report)
**Evaluator profile**: default (`.moai/config/evaluator-profiles/default.md`) — flat weighted-percentage mode (no `evaluator_mode: hierarchical` set)
**Date**: 2026-09-03

## Overall Verdict: PASS

## Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 95/100 | PASS | All 13 AC (AC-ORDER-037~049) traced to real, independently-re-run tests. `npx vitest run` on the 7 SPEC test files: `Test Files 7 passed (7)` / `Tests 138 passed (138)`. Full suite (`npm test`, this audit's own run): `Test Files 75 passed \| 1 failed (76)` / `Tests 944 passed \| 1 failed (945)` — the 1 failure is `tests/integration/auth/login.test.ts` AC-AUTH-005, a pre-existing timing-flake unrelated to this SPEC (see F1). |
| Security (25%, HARD threshold) | 95/100 | PASS | No Critical/High findings. Query-level authorization confirmed by direct code+test read (not mocked away) for all three lookup paths. Redaction verified both statically (type) and dynamically (runtime string-search test against `pk_live_super_secret` / `idem-key-super-secret` literals) — see below. Rate limit config (`WINDOW_MS=60_000`, `MAX_REQUESTS_PER_WINDOW=5`, `LOCKOUT_MS=15*60_000`) read directly from `src/lib/auth/rate-limit.ts:41-43`, matches `AC-041-EXCL-RATELIMIT` exactly. `grep -rn "dangerouslySetInnerHTML\|eval(\|new Function("` over the new files: 0 matches. `grep` for hardcoded secrets over the new files: 0 matches. |
| Craft (20%) | 93/100 | PASS | Coverage measured directly (not trusted from progress.md): `npx vitest run --coverage` scoped to the 7 SPEC files + SPEC-owned source paths → **98.09% stmts / 93.43% branch / 100% funcs / 98.09% lines** (threshold 85%). `npx tsc --noEmit` exit 0. `npm run lint` exit 0. Minor gap: `OrderLookupForm.tsx:67` (network-failure catch branch) uncovered — non-blocking (see F3). |
| Consistency (15%) | 95/100 | PASS | New repository functions (`findOrderByNumberAndPhone`, `findOrderByNumberForGuest`) follow the exact `findOrderForGuest()` single-`findFirst`-with-owner-condition-in-`where` pattern verbatim, confirmed by direct code read. The API route is a thin forwarder matching `POST /api/orders`'s shape (per its own docstring, confirmed true by reading the route body). `OrderDTO`/JSDoc/testing conventions match the rest of `src/features/orders/`. |

## Independently-Verified Claims (re-run, not trusted from progress.md)

Per project memory (plan-audit and progress.md claims must be checked against the actual artifact, not trusted from prose), every load-bearing claim below was independently re-executed or re-read in this session, not copied from progress.md:

1. **PRESERVE — zero diff on 3 protected surfaces.** `git diff --stat 284a492..HEAD -- src/app/checkout/complete/ prisma/schema.prisma prisma/migrations/` → empty output (0 lines). Confirmed independently.
2. **`findOrderForGuest()` body untouched.** `git diff 284a492..HEAD -- src/features/orders/repositories/order-repository.ts` shows exactly one `@@` hunk, a pure append after `findOrderForGuest()`'s closing brace — no hunk touches its body.
3. **`createOrder()` body untouched.** `git diff 284a492..HEAD -- src/features/orders/services/order-service.ts` shows 3 hunks: two at the import block (lines 7-16) and one pure append after line 725 — `createOrder()` (line 446) is not touched by any hunk.
4. **Rate-limit config matches AC-041-EXCL-RATELIMIT.** Read `src/lib/auth/rate-limit.ts:41-43` directly: `WINDOW_MS = 60_000`, `MAX_REQUESTS_PER_WINDOW = 5`, `LOCKOUT_MS = 15 * 60_000` — exact match to the acceptance.md §0 numbers (5/60s/15min).
5. **Redaction (AC-ORDER-043) — dynamic half is real, not just type-level.** `tests/unit/api/orders/lookup-route.test.ts:242-267` mocks the service to return an object literally containing `paymentKey: "pk_live_super_secret"`, `idempotencyKey: "idem-key-super-secret"`, `guestId: "guest-super-secret"`, and asserts the actual serialized JSON response body does not match those patterns. This is a genuine runtime regex-search test, not a compile-time-only guarantee. The M3 RED log in progress.md (`expected '...' not to match /paymentKey/`) is corroborated by re-reading the test file, not merely trusted.
6. **Cookie-bypass authorization (AC-ORDER-048) is query-level, not app-level filtering.** `tests/unit/orders/order-repository.test.ts:264-278` asserts `findOrderByNumberForGuest()` calls `prisma.order.findFirst` **exactly once** with `where: { orderNumber, guestId }` — both conditions baked into one query, not "fetch then compare". The page-level test (`order-lookup-by-number-page.test.tsx:117-137`) additionally confirms: a different guest's cookie → `notFound()` thrown, service called zero times when no cookie present, and none of the order's rendered content (order number, product name, recipient name) appears in the DOM on the refused path.
7. **acceptance.md §2/§3 phone-normalization closure.** `git diff 284a492..HEAD -- .moai/specs/SPEC-ORDER-003/acceptance.md` confirms the §2 edge-case row was rewritten to "범위 밖으로 확정 (M4, 2026-09-03 사용자 결정)" with rationale, and all 7 §3 DoD checkboxes are now `[x]` including a new 7th item recording the scope-exclusion decision. Backlog card `t25` exists in `.moai/state/kanban/backlog.json:104`. Per the team-lead's framing, this is NOT re-flagged as a new defect — it is verified-closed exactly as claimed.
8. **AC-ORDER-039/040 indistinguishable-failure property.** `tests/unit/orders/order-service.test.ts:1086-1120` — AC-039 uses `expect(notFound).toEqual(mismatch)` (full object equality, not a partial match) between a truly-nonexistent order number and a real-order-wrong-phone case; AC-040 asserts the repository is called exactly once on both paths. Both read directly, not trusted from the PASS-matrix table alone.
9. **Forbidden-phrase closed set (AC-ORDER-044).** `tests/unit/components/order-lookup-result-view.test.tsx:87-99` uses the identical 12-string closed list from acceptance.md verbatim, iterating `it.each` over the three `OrderStatus` values.
10. **Flaky-test claim in progress.md §E.2 M2 (residual risk).** Independently reproduced: ran the full suite twice in this session (`npm test`), and the identical single test (`AC-AUTH-005` response-time comparison) failed both times with different but comparable diff/tolerance numbers (42.78ms/37.67ms and, on the prior progress.md run, 28.37ms/34.68ms — i.e., genuinely load-sensitive, not a fixed regression). Confirmed `git diff 284a492..HEAD -- tests/integration/auth/login.test.ts` is empty (this SPEC did not touch that file) and the test traces to `SPEC-AUTH-001` (commit `b551c2d`, unrelated). Re-running with `-t "AC-AUTH-005"` in isolation passed 3/3 times. This is consistent with — and independently corroborates — the flakiness progress.md itself already disclosed; it is not evidence of a regression introduced by this SPEC.

## Findings (structured defect-list, coverage-first)

All findings below are **optional** (non-blocking). There are no blocking findings — the overall PASS verdict does not rest on any fix being required.

- **F1** [Info] [optional] `tests/integration/auth/login.test.ts:121` — AC-AUTH-005 is a pre-existing, load-sensitive timing-comparison test unrelated to SPEC-ORDER-003 (zero diff on the file since SPEC-AUTH-001's commit `b551c2d`). It failed in both full-suite runs executed during this audit (944/945 each time) but passed 3/3 times when run isolated via `-t`. Required fix: none within this SPEC's scope; if repeat flakiness becomes disruptive, a follow-up SPEC could widen the tolerance or move the assertion off wall-clock timing — tracked as a pre-existing debt, not a SPEC-ORDER-003 defect.
- **F2** [Low] [optional] `src/lib/auth/rate-limit.ts` (PRESERVE, reused SPEC-AUTH-001 infrastructure, not modified by this SPEC) — `checkIpRateLimit` keys solely on the `x-forwarded-for`-derived IP; without a trusted reverse proxy that overwrites client-supplied `X-Forwarded-For`, a client can rotate the header to evade the per-origin lockout on `/api/orders/lookup`. This is an inherited property of infrastructure this SPEC explicitly reuses rather than re-designs (plan.md §0 decision 3 / §5 risk table already names the residual guessing risk this rate limit only "lowers the surface of," not eliminates). Required fix: none within this SPEC; a platform-level trusted-proxy IP-extraction hardening would be a separate infra SPEC.
- **F3** [Low] [optional] `src/components/orders/OrderLookupForm.tsx:67` — the `fetch()`-throws (network failure) catch branch is not exercised by the M2 test suite (98.97% branch coverage on this file vs 100% on most SPEC-scope files; SPEC-wide aggregate is 93.43% branch / 98.09% stmt, both comfortably above the 85% threshold). Required fix (optional): add a test that mocks `fetch` to reject and asserts the "주문을 조회하지 못했습니다..." fallback message renders.
- **F4** [Info] [optional] AC-ORDER-040 / REQ-ORDER-036's response-time channel — acceptance.md §0's `AC-040-EXCL-TIMING` marks this as a **permanent**, spec-declared exclusion (structural proxy — equal repository-call counts — substitutes for actual timing measurement). This is not a defect; it is recorded here purely for findings-stage completeness per the coverage-first instruction. No fix applicable — closing this gap would require a new SPEC establishing a timing-measurement mechanism, exactly as acceptance.md §0 already states.

## Scope Note — Phone-Normalization Gap (NOT re-flagged, per team-lead instruction)

The team-lead's brief specifically instructed this auditor not to re-raise the acceptance.md §2 phone-normalization edge case as a new defect, since it was found by the orchestrator at M4, reported to the user, and closed as an accepted residual limitation with backlog card `t25`. This audit independently confirmed (item 7 above) that acceptance.md §2's edge-case row and §3's DoD checklist were, in fact, updated to reflect that closure — the claim is verified-true, not merely asserted.

## Recommendations

- Consider F3 (network-failure branch test) as a small follow-up, not a blocker.
- F1 and F2 are pre-existing conditions inherited from other SPECs' infrastructure; no action required within SPEC-ORDER-003.
- No action required for F4 — it is a documented, permanent, spec-level scope boundary, not a gap in this implementation.

---

*This report was independently authored by sync-auditor and persisted to disk per the sync-audit self-persistence requirement (`.claude/agent-memory` policy: sync-auditor must self-persist its own report).*
