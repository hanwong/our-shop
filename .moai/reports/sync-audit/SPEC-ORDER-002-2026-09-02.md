# Sync-Audit Report — SPEC-ORDER-002

Date: 2026-09-02
Auditor: sync-auditor (independent post-implementation quality audit, `--deep` lens)
Worktree at audit time: `.claude/worktrees/t6`, branch `WT-inventory-concurrency`, HEAD `24b17b2`

> Persistence note: this file was written by the orchestrator after the fact,
> transcribing sync-auditor's actual returned verdict verbatim (from the
> Agent() task-notification result the orchestrator received) — the auditor
> was not originally instructed to persist its own report (the same
> execution gap previously found and corrected for SPEC-PAYMENT-001). No
> judgement in this file was altered, added, or re-derived; it is the
> auditor's real output, filed to its designated location per the Report
> Persistence contract.

## Evaluation Report

SPEC: SPEC-ORDER-002 ("재고 차감 동시성 제어와 품절 처리 (게스트 주문)")
Overall Verdict: **PASS**

Evaluator profile: default (`.moai/config/evaluator-profiles/default.md`), flat weighted-percentage mode (`harness.yaml` sets `evaluator_mode: final-pass`/`per-sprint`, not `hierarchical`).

### Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 95/100 | PASS | Independently re-ran `npm test` in this run, this tree (HEAD `24b17b2`): `Test Files 64 passed (64)` / `Tests 801 passed (801)` / `exit=0` (matches progress.md §E.3 `final_suite`). All 13 AC (AC-ORDER-024~036) traced to concrete tests: verified `stock-write-paths.test.ts` (AC-024, single-write-site static scan, guarded `gte`), `order-service.test.ts:559-589` (AC-025, lock order `["p-2","p-5","p-9"]` vs preserved display order `["p-9","p-2","p-5"]`), `order-service.test.ts:424-510` (AC-027/028, multi-line short-report + "never reports a bought-back quantity"), M4/M4-fix real-PostgreSQL harness (`tests/integration/orders/concurrency.postgres.test.ts:407-465`, independently read) directly applies the production `isTransactionConflict()` to a genuine PostgreSQL 40P01 deadlock error object and asserts `true`. `AC-026-EXCL-CONCURRENCY`/`AC-034-EXCL-DEADLOCK` are declared, named, capability-gated exclusions (spec's own §0 "Where" gate) closed on the dev machine and honestly reported as open in CI — not a silent gap. |
| Security (25%) | 95/100 | PASS | Ran independent grep-based OWASP probes on `src/features/orders/**` and `src/components/checkout/**`: 0 matches for `eval(`/`exec(`/`$queryRawUnsafe`/`dangerouslySetInnerHTML`; 0 matches for hardcoded secrets/API keys/passwords; 0 raw string-interpolated `$queryRaw`; input validation present and centralized (`validate()`, `isRecord()`, `requiredText()` in order-service.ts:135-204). All stock writes go through one parameterized `updateMany` with a `stock: { gte }` guard (order-repository.ts:129-139) — no read-compare-write injection surface. |
| Craft (20%) | 95/100 | PASS | Independently re-ran coverage for all 4 changed files together: `order-service.ts 96.69%/96.84% branch`, `order-repository.ts 100%`, `OrderSummary.tsx 100%/100%`, `CheckoutForm.tsx 100%/94.87% branch` — output matched progress.md's cited figures byte-for-byte. `npm run lint` → `exit 0`, no output. Naming-convention grep found 0 violations. |
| Consistency (15%) | 95/100 | PASS | Code follows established patterns verbatim: `OrderSummary.tsx`'s pure-presentation shape mirrors `ProductDetailView`; `CheckoutForm.tsx`'s `PRICE_CHANGED`-precedent handling of `INSUFFICIENT_STOCK`/`CONCURRENCY_RETRY`; repository layer's transaction-client-required-no-singleton-default convention is applied identically to the new `findStockByProductIds`. |

**Weighted score: 95/100.**

### Focus-area findings (from the prompt's 4 directed questions)

1. **`isTransactionConflict()` string/SQLSTATE matching — defensible, not blocking.** The M4 investigation *falsified* the original `code === "P2034"` assumption against a real PostgreSQL 40P01 (progress.md M4 §2-bis, independently confirmed by reading `order-service.ts:234-282` and the real-DB test at `concurrency.postgres.test.ts:407-465`). The fix anchors the regex to the structured `code: "…"` sub-string (not loose 5-digit matching) and is guarded by a live-DB assertion that applies the *production function* to a *genuine* aborted transaction — if a future Prisma reshapes the message, that test goes red rather than the predicate silently reverting to a 500. This is a well-reasoned trade-off with an active tripwire, not a defect.

2. **M1-M5 evidence blocks satisfy VCI §3 — confirmed, not narrative.** Every milestone's Claim/Evidence/Baseline-attribution/Gaps/Residual-risk section cites verbatim command output, and I independently reproduced the numbers (test counts, coverage percentages, diff-scope emptiness) exactly. The Gaps sections are unusually candid (e.g., "the service was never observed as the deadlock victim — 4/4 attempts picked the control transaction"), which is the honest-negative-space pattern §3.4 requires, not a self-report gloss.

3. **`products` display-order (lock order, not cart order) — acceptable, no AC violated.** Verified `acceptance.md` AC-ORDER-032 specifies only that each named product's available quantity appears, with no ordering requirement. Confirmed `CheckoutForm` has no cart-order context available to it (only `idempotencyKey`/`confirmedTotal`), so re-sorting would require a new prop/coupling. Documented as residual risk, not a defect.

4. **Coverage figures — verified correct, not assumed.** Independently re-ran coverage across all 4 changed files in one pass; output matched progress.md exactly (see Craft row above).

### Findings (structured defect-list)

None are blocking. All are optional/informational, already self-documented in progress.md's own Gaps/Residual-risk sections:

- F1 [Low] [optional] `order-service.ts` isTransactionConflict — message-text SQLSTATE matching is not an API contract; mitigated by a live-DB tripwire test but remains fragile to a future Prisma error-shape change. Required fix (if pursued): none required now; revisit if the tripwire test ever goes red.
- F2 [Low] [optional] `.github/workflows/ci.yml:60` — `DATABASE_URL` is still a placeholder, so `AC-026-EXCL-CONCURRENCY`/`AC-034-EXCL-DEADLOCK` stay open in CI (closed only on the dev machine). This is an explicitly accepted, user-confirmed scope decision (plan.md §0 option B) owned by a follow-up CI SPEC — not a defect of this SPEC.
- F3 [Low] [optional] `tests/integration/orders/concurrency.postgres.test.ts` — the scenario where the *service's own transaction* (not the counterfactual harness) becomes the deadlock victim was never observed end-to-end (PostgreSQL always picked the control transaction in 4 attempts). REQ-ORDER-027 is still verified via two independent paths (predicate-on-real-error + service-mapping unit tests), so this is a documented residual gap, not an unverified claim.
- F4 [Low] [optional] `CheckoutForm.tsx`/`OrderSummary.tsx` — no dedicated `CONCURRENCY_RETRY` client copy; the generic server `error` string is reused. Deliberate, reversible, documented choice (progress.md M3 §5).
- F5 [Informational] `acceptance.md` §3 Definition-of-Done checkboxes are unticked even though the underlying items pass per progress.md evidence — cosmetic document-sync item, not a run-phase functional defect; belongs to the pending sync-phase (§E.4).

### Recommendations

- No blocking fixes required before sync-phase.
- At sync-phase, manager-docs should tick the `acceptance.md` §3 DoD checkboxes that progress.md's evidence already supports (F5) and carry forward the two open CI exclusions (F2) and the un-pushed branch/no-PR state into the CHANGELOG/PR body so the lead has full context before merge.

**Verified evidence paths (independently re-executed, this run, this tree, HEAD `24b17b2`):**
- `npm test` → 64 files / 801 tests / exit 0
- `npm run lint` → exit 0, no output
- Coverage batch across `order-repository.ts`, `order-service.ts`, `OrderSummary.tsx`, `CheckoutForm.tsx` → matches progress.md exactly
- `git diff --numstat 0908b43 -- prisma src/components/product/ProductDetailView.tsx src/features/payments` → empty (PRESERVE list intact)
- `.moai/reports/plan-audit/SPEC-ORDER-002-review-1.md` (main checkout, not the worktree — gitignored per-checkout artifact) confirmed on disk with real content: PASS, 0.93, matching progress.md §E.1's claim.

## Post-audit disposition note (added by the orchestrator, clearly separated from the auditor's original output above)

F5 (unticked `acceptance.md §3` DoD checkboxes) was NOT resolved as of this filing — `acceptance.md` body content is outside manager-docs' edit authority (frontmatter `status:`/`updated:` only), so the checkboxes remain unticked. This is recorded as an explicit residual item in `progress.md` (the "알려진 잔여 항목 — sync-auditor F5" subsection, right after §E.4) and was surfaced to the Kanban lead for a re-delegation decision (tick via `manager-spec`, or accept as a documented gap). Unlike SPEC-PAYMENT-001's F1/F2/F3, this item is NOT yet resolved in a follow-up commit.
