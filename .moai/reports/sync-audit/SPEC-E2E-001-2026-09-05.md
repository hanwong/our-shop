# Sync-Audit Report — SPEC-E2E-001

SPEC: SPEC-E2E-001 ("결제·주문 경로 E2E 테스트 시나리오 — 게스트 전용")
Harness: thorough
Auditor: sync-auditor (Claude-only; no cross-model backend requested)
Audited at: 2026-09-05
Worktree: `.claude/worktrees/t14`, branch `WT-checkout-e2e-tests`, HEAD `99e2775`

## Overall Verdict: **PASS**

All must-pass dimensions (Functionality, Security) independently clear their thresholds. No blocking findings. One optional/residual-risk note (M6 cold-start flake) is documented and does not affect the verdict.

---

## Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 98/100 | PASS | Re-ran `npm run test:e2e` twice independently (not relying on progress.md's claim) — both runs 9/9 passed, exit 0. Verbatim: `9 passed (21.1s)` (run 1), `9 passed (19.4s)` (run 2). A third ad-hoc run after the AC-E2E-003 spot-check also passed (exit 0). No cold-start flake reproduced in this worktree (consistent with progress.md's own hypothesis that the flake was specific to a brand-new worktree's first `next dev` compile — this worktree already has a warm `.next` cache). Spot-checked AC-E2E-006/007/008 directly against `e2e/m3-happy-path.spec.ts` source — all three assertions exist exactly as progress.md describes (role-based add-to-cart → `/cart` reflection → checkout form submit → `/checkout/complete/{orderId}` → payment trigger visible → payment success → `role=status` "결제가 완료되었습니다" visible, trigger button hidden, `tossHostHits` length 0). Independently re-executed AC-E2E-003 by temporarily removing `e2e/e2e-stub.env`: suite failed exit 1 *before* any scenario ran, with the exact message `required environment variable(s) missing before suite start: NEXT_PUBLIC_PG_CLIENT_KEY, PG_SECRET_KEY` — restored the file and re-confirmed the suite is green (exit 0) afterward. |
| Security (25%) | 96/100 | PASS | `e2e/e2e-stub.env` contains only `test_ck_e2e_stub_...` / `test_sk_e2e_stub_...` placeholder values with an inline comment explaining server-side confirm/query calls are intercepted before leaving the process — no real secret material. `tossHostHits` guard verified present in `e2e/support/fixtures.ts` (lines 4, 21, 27, 36) and is actually asserted (not just defined) — `grep -c tossHostHits e2e/*.spec.ts` shows 2/5/3/7/2 hits across the 5 spec files, i.e. every payment-adjacent scenario asserts `toHaveLength(0)`. `npm audit --omit=dev --json` on this branch: `{moderate:3, high:5, critical:1, total:9}` — matches the orchestrator's claimed baseline exactly. Independently confirmed (not merely accepted) the baseline-unchanged claim: diffed the vulnerable-package version strings (`@mapbox/node-pre-gyp`, `tar`, `uuid`, `gaxios`) between `origin/main`'s `package-lock.json` and this branch's — zero diff, confirming this SPEC introduced none of the 9 pre-existing vulnerabilities. |
| Craft (20%) | 90/100 | PASS | `npm run lint` → exit 0, no output. `npm run typecheck` → exit 0, no output. `npm test` (vitest baseline) → exit 0, `110 files / 1478 tests` — matches REQ-E2E-002's non-regression requirement exactly, re-confirmed independently. Error handling in mock/stub code reviewed (`toss-sdk-stub.js`'s orderId-derived paymentKey fix for the M2 P2002 collision is structurally sound — confirmed the fix is present in source: `u.searchParams.set("paymentKey", "e2e_stub_payment_key_" + options.orderId)`). Minor deduction: the one CSS-selector pair (`dt:has-text(...) + dd`) in `e2e/m4-edge-cases.spec.ts` is a documented, justified exception (no accessible-name equivalent exists for a `<dl>` definition list without adding test-only markup) rather than a defect — not scored as a blocking issue, but noted as the reason score is 90 rather than higher. The noted M6 cold-start flake (1/4 runs failed on a brand-new worktree's first compile, 3/4 clean) is a residual risk, not a blocker — not reproduced in this independent re-verification across 3 runs in a warm worktree. |
| Consistency (15%) | 95/100 | PASS | Playwright patterns consistent with plan.md §B/§C/§D decisions: undici `setGlobalDispatcher` interception confirmed via M1 spike evidence in progress.md and confirmed structurally present (`e2e/support/mock-toss-api.mjs` `@MX:WARN`-tagged, loaded via `NODE_OPTIONS='--import ...'` in `playwright.config.ts`). Selector audit: `grep -n '\.locator(\|data-testid\|css=\|xpath='` across all 5 spec files + support files returns exactly 2 matches, both in `e2e/m4-edge-cases.spec.ts`'s coupon scenario, both documented with rationale in progress.md §E.2 M5 — matches the claimed "only 2 CSS selectors documented with rationale" exactly. All @MX tags verified present exactly as claimed (see Findings/MX section below). |

---

## Independent Verification Log (commands actually run, verbatim)

1. `git diff --stat origin/main...HEAD -- src/` → **empty output** (zero production-source diff confirmed independently).
2. `git diff --stat origin/main...HEAD -- . ':!e2e' ':!.moai/specs/SPEC-E2E-001'` → only `.gitignore`, `package-lock.json`, `package.json`, `playwright.config.ts` changed (162 insertions / 6 deletions) — no unexpected `src/` or other surface touched.
3. `npm run test:e2e` (run 1) → exit 0, `9 passed (21.1s)`.
4. `npm run test:e2e` (run 2) → exit 0, `9 passed (19.4s)`.
5. `npm test` (vitest) → exit 0, `Test Files 110 passed (110)` / `Tests 1478 passed (1478)`.
6. `npm run lint` → exit 0, no findings.
7. `npm run typecheck` → exit 0, no findings.
8. `npm audit --omit=dev --json` → `{moderate:3, high:5, critical:1, total:9}`; cross-checked vulnerable package version strings against `origin/main`'s lockfile — identical (baseline confirmed unchanged, not merely accepted on the orchestrator's attribution).
9. AC-E2E-003 spot-check: removed `e2e/e2e-stub.env`, ran `npm run test:e2e` → exit 1, verbatim message names both missing vars before any scenario runs; restored file, re-ran → exit 0 (suite green again).
10. `grep` spot-checks: MX tags (ANCHOR/WARN×2/NOTE/TODO) all present at claimed locations; `tossHostHits` guard present and actively asserted in all payment-adjacent scenarios; CSS-selector count exactly 2, both documented; plan-audit report file exists on disk (`43358` bytes) with verbatim `iter1 FAIL 0.825` → `iter2 PASS 0.94` matching progress.md's claim exactly (not merely trusting the progress.md prose).
11. Read `e2e/m3-happy-path.spec.ts` in full — AC-E2E-006/007/008 assertions verified to exist exactly as progress.md's PASS matrix describes.

---

## Findings (structured defect-list)

No blocking findings.

- F1 [info] [optional] `e2e/m4-edge-cases.spec.ts:178,191` — Two CSS selectors (`dt:has-text(...) + dd`) remain in the coupon scenario because `OrderSummary.tsx`'s `<dl>` definition-list markup has no accessible-name equivalent for associating a `dt`/`dd` pair without adding test-only ARIA attributes to the app. This is already documented in progress.md §E.2 M5 with the same rationale independently re-confirmed here. Optional fix (not required): if a future SPEC touches `OrderSummary.tsx`, consider `aria-labelledby` wiring so all E2E selectors can be accessible-name-based. Not blocking — the existing markup is legitimate untouched production code and modifying it purely for test convenience would violate scope discipline.
- F2 [info] [optional] The M6 cold-start flake (1 failure in 4 runs on a brand-new worktree's first `next dev` compile, `SyntaxError: Unexpected end of JSON input`) is a documented residual risk in progress.md, not reproduced in this independent audit's 3 runs (this worktree already had a warm `.next` build cache from the run-phase sessions). No action required; flagging for visibility only in case it recurs in a genuinely fresh CI-like environment in a future SPEC.

---

## Recommendations

- No blocking fixes required. The SPEC is ready for sync-phase documentation and PR creation.
- (Optional, non-blocking) If a future SPEC introduces CI-based E2E execution (currently explicitly out of scope per spec.md §3), consider running the suite against a genuinely cold `.next` build at least once during that follow-up SPEC's validation to determine whether the M6 cold-start flake is a real intermittent hazard worth a `webServer.timeout` increase, or was a one-off artifact of that particular worktree provisioning.
