# Sync-Audit Report — SPEC-AUTH-004

Date: 2026-09-05
Worktree: `.claude/worktrees/t44` (branch `WT-staff-header-fix`, HEAD `6e08cb1`)
Auditor: sync-auditor (independent, re-executed all claims — did not trust progress.md prose)

## Overall Verdict: **PASS**

All 9 acceptance criteria (AC-AUTH-048 ~ 056) independently re-verified and hold. Full suite green (113 files / 1493 tests), `tsc --noEmit` and `npm run lint` both exit 0. The core structural fix (header moved from root layout to `(shop)/layout.tsx`, staff routes have no layout of their own) is confirmed by direct source inspection, not inference. PRESERVE list (8 targets incl. `SiteHeader.tsx` and `src/app/staff/`) is zero-diff against `origin/main`. No blocking findings.

---

## Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 98/100 | PASS | `npx vitest run --reporter=dot` → `Test Files 113 passed (113)` / `Tests 1493 passed (1493)`. `find src/app -name "layout.tsx"` → exactly `src/app/layout.tsx` + `src/app/(shop)/layout.tsx`. `grep -c "SiteHeader" src/app/layout.tsx` → exit 1 (0 matches). `(shop)/layout.tsx` read directly: imports `SiteHeader` from `@/components/layout/SiteHeader`, renders `<SiteHeader />` then `{children}` inside a fragment. `find src/app/staff -name "layout.tsx"` → no output (no file). All 9 AC re-verified below. |
| Security (25%) | 92/100 | PASS | The exposure path is closed by a mechanical structural-composition proof (3 independently-pinned facts in `shell.test.tsx`: AC-048 staff has no layout, AC-049 root layout renders no SiteHeader by both element-tree check and 0-occurrence source scan, AC-051 exactly one layout renders SiteHeader). This is stronger than a behavioral test — a future layout added between root and any customer route would fail AC-051 immediately. Root cause (`POST /api/auth/logout` role-unaware revocation) is honestly and explicitly disclosed as UNRESOLVED in `spec.md` §1.2/§3/§4 and `progress.md` residual-risk item 4 — no overclaiming found anywhere in the artifacts. Not 100 only because the underlying vulnerability genuinely persists (by design/scope, and honestly labeled as a forward pointer) — this is a scope decision, not a defect in this SPEC's own work. |
| Craft (20%) | 95/100 | PASS | Both spec-level tensions were resolved cleanly, in-scope, and did not require a blocker/re-delegation to manager-spec: (1) the `@MX:WARN` comment vs AC-AUTH-049's 0-occurrence requirement was resolved by rewording the comment to warn clearly without spelling out the component name — verified by direct read, the wording ("do not (re-)add the shared login-state header component...") still unambiguously warns future editors; (2) the AC-AUTH-054 exact-12-file-count vs the M4 new-guard-file draft was resolved by folding 3 new tests into the existing `shell.test.tsx` (one of the already-counted 12) rather than adding a 13th file — verified: `git diff --stat` shows exactly 12 changed test files, no 13th added. RED evidence for the M4 guards is genuine (2 real failures observed pre-fix, documented verbatim). |
| Consistency (15%) | 93/100 | PASS | Both disclosed shortcuts are legitimate and independently verified, not just asserted: `git reflog show m2m5-route-move` shows `2891974 m2m5-route-move@{1}: commit (amend): feat(SPEC-AUTH-004): M4 ...` confirming the amend actually happened, pre-push (the amended commit is the one that was ultimately pushed/merged — no evidence of amending an already-pushed commit). The `SKIP_MOAI_PRECOMMIT=1` override on the M2 commit has a documented, verifiable reason (M2 intentionally leaves 12 test files with broken module resolution until M3, per plan.md §F's explicit M2/M3 split) — this is an expected, pre-approved mid-milestone breakage, not a hidden defect. |

---

## Independent Re-Verification Detail (commands run, not summarized)

### 1. Test suite
```
$ npx vitest run --reporter=dot
 Test Files  113 passed (113)
      Tests  1493 passed (1493)
```
Matches claim exactly (113 files, same count as before the SPEC; 1493 tests, +4 net over the 1489 baseline — 1 from shell.test.tsx assertion split, 3 from new M4 guards).

### 2. tsc / lint
```
$ npx tsc --noEmit    → exit 0, no output
$ npm run lint        → exit 0, "eslint ." with no warnings/errors
```

### 3. Core fix mechanism
```
$ find src/app -name "layout.tsx"
src/app/(shop)/layout.tsx
src/app/layout.tsx
```
Exactly 2, as claimed.

```
$ grep -c "SiteHeader" src/app/layout.tsx
0   (exit 1)
```
Root layout source contains the string "SiteHeader" nowhere — confirmed by full-file read (`cat -n src/app/layout.tsx`), including the `@MX:WARN`/`@MX:NOTE` comment blocks, which deliberately avoid the literal name while still warning clearly against re-adding "the shared login-state header component."

`src/app/(shop)/layout.tsx` read in full: imports `SiteHeader`, renders `<SiteHeader />` then `{children}` inside a `<>...</>` fragment — matches AC-AUTH-050's exact Given-When-Then.

```
$ find src/app/staff -name "layout.tsx"
(no output)
```
Confirmed — staff has no layout.tsx of its own, so it inherits only the header-free root layout.

### 4. PRESERVE list (8 targets), all individually verified zero-diff against `origin/main`
```
git diff --stat origin/main...HEAD -- src/middleware.ts                              → (empty)
git diff --stat origin/main...HEAD -- src/lib/auth/session-resolver.ts               → (empty)
git diff --stat origin/main...HEAD -- src/features/admin/services/admin-session.ts   → (empty)
git diff --stat origin/main...HEAD -- src/app/api/auth/logout/route.ts               → (empty)
git diff --stat origin/main...HEAD -- src/components/layout/SiteHeader.tsx           → (empty)
git diff --stat origin/main...HEAD -- src/components/layout/LogoutButton.tsx         → (empty)
git diff --stat origin/main...HEAD -- prisma/schema.prisma                           → (empty)
git diff --stat origin/main...HEAD -- src/app/staff                                  → (empty)
```
All 8 empty. `find src/app/staff -type f | wc -l` → 13 (unchanged file count).

### 5. SiteHeader.tsx's 3 stale @MX comments deliberately untouched
```
$ grep -n "@MX:" src/components/layout/SiteHeader.tsx
11: * @MX:ANCHOR rendered by layout.tsx on every route — the sole layout-level
14: * @MX:REASON layout.tsx renders this on every route via the root layout, so
18: * @MX:NOTE calls resolveSession() (src/lib/auth/session-resolver.ts)
```
Confirmed: the file is untouched (zero diff, item 4 above) and the 3 comments — now factually stale (the header no longer renders "on every route" nor "via the root layout") — were deliberately left as-is per plan.md §G anti-pattern 9. This is the correct call: touching this PRESERVE-pinned file for any reason, including comment accuracy, would break AC-AUTH-056's zero-diff requirement.

### 6. Route-move rename detection (10 files)
```
$ git diff --stat origin/main...HEAD -- src/app/
 src/app/{ => (shop)}/cart/page.tsx                         | 0
 src/app/{ => (shop)}/checkout/complete/[orderId]/page.tsx  | 0
 src/app/{ => (shop)}/checkout/page.tsx                     | 0
 src/app/(shop)/layout.tsx                                  | 29 +
 src/app/{ => (shop)}/login/page.tsx                        | 0
 src/app/{ => (shop)}/orders/lookup/[orderNumber]/page.tsx  | 0
 src/app/{ => (shop)}/orders/lookup/page.tsx                | 0
 src/app/{ => (shop)}/page.tsx                               | 0
 src/app/{ => (shop)}/products/[productId]/not-found.tsx     | 0
 src/app/{ => (shop)}/products/[productId]/page.tsx          | 0
 src/app/{ => (shop)}/signup/page.tsx                        | 0
 src/app/layout.tsx                                          | 37 +-
```
All 10 moved files show as clean `{ => (shop) }` renames with 0/0 diff, exactly as claimed. `(shop)/layout.tsx` correctly appears as a genuine new file (29 insertions), not a rename artifact.

### 7. Spot-checked 3 of the 9 test-source claims directly
- `shell.test.tsx` — read in full. Contains exactly the 3 new guard `describe` blocks claimed (AC-048, AC-049 static-scan half, AC-051), plus the `ShopLayout — AC-AUTH-050` block and the rewritten `RootLayout` "does not render SiteHeader" test. All assertions match the described behavior exactly; ran in isolation (`npx vitest run tests/unit/app/shell.test.tsx --reporter=verbose`) → all 9 tests pass.
- `tests/unit/auth/auth-boundary-static.test.ts` diff — only the 2 `FILES` array path strings changed (`src/app/login/page.tsx` → `src/app/(shop)/login/page.tsx`, same for signup); `CLIENT_STATE_PATTERN` and all assertions untouched.
- `tests/unit/app/product-detail-page.test.tsx` diff — only 4 path-string changes (`@/app/products/...` → `@/app/(shop)/products/...`, `"src/app/products"` → `"src/app/(shop)/products"` ×2); mocks, assertions, and forbidden-token scan list untouched.
- Also spot-checked `checkout-page.test.tsx` (not required, done for extra coverage): same pattern, path-only.

### 8. `@MX:WARN` comment wording assessment
Read `src/app/layout.tsx` in full. The rewritten `@MX:WARN` block reads: *"do not (re-)add the shared login-state header component to this root layout. Doing so would make `/staff/**` inherit the customer header again, reintroducing SPEC-AUTH-004's defect (an admin ending their own session via the customer logout button)."* This clearly and specifically warns against the exact regression, without ever spelling out "SiteHeader" — satisfying AC-AUTH-049's 0-occurrence requirement while preserving the warning's clarity and specificity. No degradation in warning quality versus a version that named the component directly.

### 9. Residual-risk disclosure honesty
Confirmed by direct read of `spec.md` §1.2 ("실제 위험 — 관리자 세션의 무자각 종료"), §3 Out-of-Scope ("`logout/route.ts`의 역할 비인지 폐기 동작 수정"), §4 forward pointer ("로그아웃 역할 인지 SPEC"), and `progress.md` §E.1 residual-risk item 4. None of these overclaim resolution — every one explicitly states the root cause is NOT fixed by this SPEC, only the exposure path (visible logout button on staff pages) is removed. This is accurate and matches the actual code change (logout route itself has zero diff, confirmed in item 4 above).

### 10. Commit-strategy honesty
```
$ git reflog show m2m5-route-move
80d17b3 ...@{0}: commit: feat(SPEC-AUTH-004): M5 full verification ...
2891974 ...@{1}: commit (amend): feat(SPEC-AUTH-004): M4 new regression guards ...
ac7b30c ...@{2}: commit: feat(SPEC-AUTH-004): M4 new regression guards ...  ← pre-amend, orphaned
23ea372 ...@{3}: commit: feat(SPEC-AUTH-004): M3 test path alignment ...
ec52ea0 ...@{4}: commit: feat(SPEC-AUTH-004): M2 route move ...
```
The amend is independently confirmed via reflog (not just asserted in progress.md) — it happened before any push (the orphaned pre-amend `ac7b30c` never made it to any remote-tracked history). `SKIP_MOAI_PRECOMMIT=1` on the M2 commit is a documented, legitimate override: M2 is a pure `git mv` that intentionally leaves 12 test files' module resolution broken until M3 fixes the import paths (plan.md §F explicitly splits M2/route-move from M3/test-path-alignment as separate milestones) — this is expected, pre-planned breakage, not a hidden defect being smuggled past the gate.

---

## Findings

No blocking findings.

- **F1** [info] [optional] `progress.md` residual-risk item 3 — the `(shop)` group name choice (includes `/login`, `/signup` which aren't strictly "shop" routes) is self-flagged as a naming-precision tradeoff with low reversal cost. No action needed; already disclosed honestly, not a defect.
- **F2** [info] [optional] The 3 now-stale `@MX:` comments in `SiteHeader.tsx` (confirmed stale in item 5 above) are correctly left untouched per the PRESERVE constraint, but represent accumulating documentation debt for a future SPEC to address (already flagged as a follow-up candidate in plan.md §E). No action needed for this SPEC.

## Recommendations

- None blocking. The two informational findings above are already tracked as intentional forward-pointers in the SPEC artifacts; no further action required before sync/PR.
