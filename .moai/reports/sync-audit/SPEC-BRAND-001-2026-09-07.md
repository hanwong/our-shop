# Sync-phase Audit Report — SPEC-BRAND-001

**SPEC**: SPEC-BRAND-001 ("OUR" 수제화 브랜드 전환 — 브랜드 아이덴티티·내비게이션·신규 페이지)
**Tier**: L (Conditional Design Route: plan → design → run)
**Audited HEAD**: `535826a` (branch `WT-our-brand-pivot`, worktree `.claude/worktrees/t61`)
**Lens**: `--deep` (Tier L + full brand pivot + long correction history)
**Auditor**: sync-auditor (Claude-only; no MCP cross-model backend invoked)

## Overall Verdict: PASS-WITH-DEBT

All 25 acceptance criteria (AC-BRAND-001..025) independently re-verified PASS. Both must-pass
dimensions (Functionality, Security) clear their thresholds comfortably. The "debt" in this
verdict is exactly the debt the run/correction-cycle sessions already disclosed and asked to
carry forward (2 legitimate PROVISIONAL items) plus two newly-found stale-comment instances of
the same class as the one this dispatch flagged, and one documented-but-unimplemented visual
design decision. None of these are blocking; none contradict any REQ or AC as literally written.

## Dimension Scores

| Dimension | Score | Verdict | Evidence |
|-----------|-------|---------|----------|
| Functionality (40%) | 92/100 | PASS | Full AC-BRAND-001..025 re-verification below. `npx vitest run --reporter=dot` (this session, independent re-run): `Test Files 130 passed (130)` / `Tests 1665 passed (1665)` — matches progress.md §E.2b's post-correction-cycle claim exactly, and the previously-flaky `AC-AUTH-021`/`AC-AUTH-005` tests passed cleanly in this run too (`✓ tests/integration/auth/login.test.ts (1 test) 19969ms` — `AC-AUTH-005` — median diff 26.43ms < tolerance 51.25ms). Deductions: F3 (documented design.md §3.1 visual decision not implemented in shipped code) |
| Security (25%) | 96/100 | PASS | `git diff 9e788e9..HEAD --stat -- package.json package-lock.json` → only `package.json \| 2 +-, 1 file changed, 1 insertion(+), 1 deletion(-)` (the `name` field only — zero new dependencies). Secret scan across every SPEC-touched file: `grep -rniE '(api[_-]?key\|secret\|password)\s*=\s*["\x27][a-zA-Z0-9]{8,}' "src/app/(shop)/shop" "src/app/(shop)/bespoke" "src/app/(shop)/story" src/components/layout prisma/seed-products.ts` → exit 1 (no matches). `src/lib/auth/jwt.ts` `ISSUER`/`AUDIENCE` confirmed unchanged (`"our-shop"` / `"our-shop-api"` still literal, as REQ-BRAND-004 requires) — the only residual `our-shop` string in the entire touched-surface grep, and it is a required residual, not a miss. CSRF/session/order boundary diff empty (orchestrator's pre-audit batch, independently spot-checked by re-reading `SiteHeader.tsx` — `resolveSession()`/`cookies()` call untouched). |
| Craft (20%) | 85/100 | PASS | `npx tsc --noEmit` → exit 0 (no output, this session's independent run). `npx eslint .` → exit 0 (no output, this session's independent run). Test coverage for every new file is dense and behavior-locked (property-style uniform-disabled-state test for `SizeSelector`, source-level forbidden-token scan for `/bespoke`+`/story`, mocked-arbitrary-category proof for `/shop`'s derivation claim). Deductions: F2 (stale PROVISIONAL comment in `BrandLogo.tsx`), F4 (`.plate` CSS class defined but never applied — dead code) |
| Consistency (15%) | 85/100 | PASS | Component naming (`SiteHeaderNav.tsx`, `BrandLogo.tsx`, `SizeSelector.tsx`) matches existing `PascalCase` file-per-component convention (`ProductGallery.tsx`, `AddToCartButton.tsx`, ...); `@MX:ANCHOR`/`@MX:NOTE` present on the high-fan-in `SiteHeader.tsx` per the MX Tag Quality Gate (`>=3 callers` rule N/A here — single layout consumer — but tagged anyway for the session-branch regression risk it anchors). Deductions: F1 (confirmed stale comment, the dispatch's flagged finding) + F2 (a second, independently-found instance of the same doc-drift class) — comments no longer tracking the current `design.md` resolution state is a repeating pattern, not a one-off. |

**Must-pass firewall**: Functionality (92) and Security (96) both clear the Tier L default 85%
must-pass threshold independently. No must-pass dimension fails, so no dimension forces an
overall FAIL regardless of the others.

**Weighted harmonic mean**: 1 / (0.40/92 + 0.25/96 + 0.20/85 + 0.15/85) ≈ **90.3/100**.

## AC-by-AC independent re-verification (all 25)

Every row below is this session's own command + observed output, not a re-statement of
progress.md's self-report.

| AC | Verdict | Evidence (this session, independent) |
|----|---------|----------------------------------------|
| AC-BRAND-001 | PASS | `grep -rln "our-shop" src package.json` → only `src/lib/auth/jwt.ts` (required residual, REQ-BRAND-004). `grep -n "title\|description" src/app/layout.tsx` → `title: "OUR \| 수제 구두"`, description mentions 수제화 브랜드 OUR. |
| AC-BRAND-002 | PASS | `grep -n "OUR\|our-shop" "src/app/(shop)/page.tsx"` → `<h1 ...>OUR</h1>` only, no `our-shop`. |
| AC-BRAND-003 | PASS | `grep -n '"name"' package.json` → `"name": "our"`. |
| AC-BRAND-004 | PASS | `grep -n "ISSUER\|AUDIENCE" src/lib/auth/jwt.ts` → `const ISSUER = "our-shop"; const AUDIENCE = "our-shop-api";` unchanged; `git diff 9e788e9..HEAD --stat -- src/lib/auth/jwt.ts` → no output (file not in diff). |
| AC-BRAND-005 | PASS | `ls -la public/brand/` → `logo_mono_black.png` present (521 bytes). |
| AC-BRAND-006 | PASS | `BrandLogo.tsx` read in full: `<Image src="/brand/logo_mono_black.png" alt="OUR" width={40} height={40} />` — non-empty `alt`, explicit `width`/`height`, `next/image`. |
| AC-BRAND-007 | PASS | `@theme` property-**name** set diff: `git show 9e788e9:src/app/globals.css \| grep -oE '\-\-[a-zA-Z0-9-]+:' \| sort -u` vs current, `diff` → empty (0 changes, exit 0). Font tokens: `grep -n "font-heading:\|font-body:\|font-heading-weight:" src/app/globals.css` → `var(--font-heading-nf), ...` / `var(--font-body-nf), ...` / `600` — literal `"Cormorant Garamond"`/`"Lora"` NOT present. `accent-2-` lamp: `grep -c 'accent-2-' src/app/globals.css` → `0`. |
| AC-BRAND-008 | PASS | `grep -n -- "--color-bg:\|--color-surface:\|--color-text:\|--color-accent:\|--color-accent-2:" src/app/globals.css` → `#f2f2f2` / `#e9e9e9` / `#1f1f1f` / `#2b2b2b` / `#2b2b2b` — exact match to spec. Neutral/accent ramps read verbatim from file, exact match to `research.md` §4.1 / `acceptance.md` AC-BRAND-008 values. |
| AC-BRAND-009 | PASS | `--color-divider: color-mix(in srgb, #1f1f1f 16%, transparent)`. `--shadow-sm/md/lg` all use `#1f1f1f` base with unchanged geometric args (`14%`/`16%`/`22%`, `0 1px 2px` / `0 3px 10px` / `0 12px 32px`). Forbidden-hex scan: `grep -n "#b68235\|#ac803e\|#201f1d\|#2d2b2b" src/app/globals.css` → exit 1, no matches. |
| AC-BRAND-010 | PASS | `.plate { filter: grayscale(1) contrast(1.05); }` present verbatim (lines 82-84). *(See F4 — the rule exists and is correct, but is never applied anywhere in JSX; AC-BRAND-010 only tests the CSS rule's existence, which is satisfied.)* |
| AC-BRAND-011 | PASS | `SiteHeader.tsx` renders `<BrandLogo />` + `<SiteHeaderNav sessionBranch={sessionBranch} />`; `SiteHeaderNav.tsx`'s `NAV_LINKS` = SHOP(`/shop`)/BESPOKE(`/bespoke`)/STORY(`/story`)/CART(`/cart`), all four rendered via `.map()`. |
| AC-BRAND-012 | PASS | `SiteHeader.tsx`'s `sessionBranch` ternary: `session === null` → `<Link href="/login">로그인</Link>` only; else → `<span>내 정보</span>` + `<LogoutButton />` only. Mutually exclusive, matches SPEC-AUTH-003 REQ-AUTH-038~043 exactly (comment confirms "byte-for-byte unchanged from SPEC-AUTH-003 — only relocated"). |
| AC-BRAND-013 | PASS | `grep -rln "SiteHeader" src/app --include="*.tsx"` → `(shop)/layout.tsx` (the real import) and `(shop)/bespoke/page.tsx` (confirmed by direct read: line 12 is a doc-comment string "there is not even a nav `<a>` in the body (SiteHeader's nav lives one level up...)", not an import statement). `grep -rln "SiteHeader" src/app/staff` → exit 1, no matches. |
| AC-BRAND-014 | PASS | `/shop/page.tsx` renders `<ProductGrid>`, a category-filter `<div role="group" aria-label="카테고리 필터">`, and sort controls — confirmed by direct file read. |
| AC-BRAND-015 | PASS | `listProducts(query)` receives the `?category=` value verbatim via `URLSearchParams`; `shop-page.test.tsx` mocks this and asserts a proper-subset result (existing test suite, `npx vitest run` includes this file, passing). |
| AC-BRAND-016 | PASS | `shop-page.test.tsx` mocks `findAllCategories` with an **arbitrary** 2-category set (`가나다`/`ganada`, `라마바`/`ramaba` — not the real derby/loafer/boots seed) specifically to prove the rendered filter buttons come from that call, not a hardcoded list — this is the correct proof shape for a "not hardcoded" AC. Source read confirms `/shop/page.tsx` calls `findAllCategories()` and `.map()`s the result with no literal category array in the file. |
| AC-BRAND-017 | PASS | `/shop/page.tsx` imports `listProducts` from the existing `@/features/catalog/services/product-service` — no new repository/service function file created for this SPEC (confirmed by directory read: no new files under `src/features/catalog/`). |
| AC-BRAND-018 | PASS | `bespoke-story-pages.test.tsx` asserts all four landmarks (`/285mm/`, `/330mm/`, `/손으로 꿰맵니다/`, `/약 4주/`, `/15만원/`, `/30만원/`) against the rendered `BespokePage` — confirmed present in `bespoke/page.tsx`'s `<dl>` by direct read. |
| AC-BRAND-019 | PASS | `story/page.tsx` renders "한 켤레에 나흘" heading + the confirmed live-source body copy ("갑피를 꿰매고..."); `bespoke-story-pages.test.tsx`'s `StoryPage — AC-BRAND-019` test asserts both landmarks and passes (part of the 130/1665 green run). |
| AC-BRAND-020 | PASS | `bespoke-story-pages.test.tsx`'s forbidden-source-pattern scan (`<form`, `type="submit"`, cart-add copy, `"use client"`, `useState`, `onClick`) plus a rendered-DOM check (`form, button, input, textarea, select` → 0 elements) both pass against the live source files — confirmed by direct read of both page files: zero interactive elements beyond the shared layout's nav. |
| AC-BRAND-021 | PASS | `prisma/seed-products.ts` read in full: 6 `SEED_PRODUCTS` with exact prices (285000/240000/162000/198000/276000/225000) matching `research.md` §3.1 verbatim; 3 `SEED_CATEGORIES` (derby/loafer/boots); no `i % 3`-derived size-range string field anywhere in the `SeedProduct` interface or the `create`/`update` payloads. |
| AC-BRAND-022 | PASS | Script structure: `Category.upsert({ where: { slug } })` and `Product.upsert({ where: { id: fixed-deterministic-id } })` — both keyed for idempotent re-run by construction (fixed ids, no auto-increment dependency); consistent with progress.md §E.2's independently-observed dry-run (category/product counts stable across two consecutive runs). |
| AC-BRAND-023 | PASS | `SizeSelector.tsx`: `const disabled = stock === 0;` applied identically to every one of the 10 mapped `<button>` elements — a mixed per-size state is structurally impossible (single boolean, no per-item branching). |
| AC-BRAND-024 | PASS | `npx vitest run --reporter=dot` (this session) → `Test Files 130 passed (130)` / `Tests 1665 passed (1665)`, zero failures — matches (and slightly improves on, by re-confirming the previously-flaky tests) progress.md's own claim. `npx tsc --noEmit` → exit 0. `npx eslint .` → exit 0. |
| AC-BRAND-025 | PASS | `git diff 9e788e9..HEAD --stat -- prisma/migrations/` → empty (exit 0, no output). `npx prisma validate` → "The schema at prisma/schema.prisma is valid 🚀". |

## Stale-comment finding — CONFIRMED (as flagged in the dispatch)

**Confirmed.** `src/app/(shop)/story/page.tsx` lines 13-17 read (verbatim, this session's read):

```
 * Note: this source states the size range as "240–330mm", which conflicts
 * with /bespoke's REQ-BRAND-017/AC-BRAND-018-locked "285mm부터 330mm까지"
 * wording — see design.md §2.5 for the reconciliation note (unresolved,
 * left to a SPEC-body-owning follow-up; /bespoke intentionally NOT changed
 * here, see this SPEC's progress.md correction-cycle entry).
```

`design.md` §2.5 (updated in the SAME commit, `535826a`, as this file's own last touch — confirmed
by `git log`) now states the opposite conclusion: the two values are **not** a conflict — they
describe different scopes (240–330mm = whole product-line range; 285–330mm = the bespoke-only
extended-size range) — and explicitly says no REQ/AC or code change is needed. The code comment
was not updated to match in the same commit that updated the doc. This is F1 below (non-blocking).

## Findings (structured defect list)

- **F1** [Low] [optional] `src/app/(shop)/story/page.tsx:13-17` — Comment describes the
  `/story` vs `/bespoke` size-range values as an "unresolved" conflict "left to a SPEC-body-owning
  follow-up", but `design.md` §2.5 (updated in the same commit) resolved this as *not* a conflict
  (different scopes: whole-line range vs bespoke-only range). Required fix: replace the comment
  block with a short note summarizing the resolved "not a conflict, different scopes" state,
  citing `design.md` §2.5, so a future reader does not re-open an already-closed question.

- **F2** [Low] [optional] `src/components/layout/BrandLogo.tsx:5-8` (top-of-file doc comment) —
  States: *"design.md §8.3 lists `logo_mono_black.png` as PROVISIONAL (pixel content unverified —
  the filename choice rests on naming-safety inference, not on an actual review of the live
  Claude Design source)."* This is now stale: `design.md` §2.3 / §8.3 item 1 record the logo file
  as **✅ CONFIRMED** — the sync-phase correction cycle obtained live DesignSync access and
  pixel-verified the asset (`progress.md` §E.2b item 2: *"Logo file CONFIRMED ... pixel-verified
  by the sync session against the live source. No code change."*). This is the **same class of
  defect as the dispatch-flagged F1** (a doc/design-state update that did not propagate to a code
  comment describing the identical fact), found independently in a second file — worth noting as
  a repeating pattern rather than a one-off. Required fix: update or remove the PROVISIONAL
  framing in this comment to reflect the CONFIRMED status.

- **F3** [Low] [optional] `src/components/product/ProductDetailView.tsx` / `SizeSelector.tsx` —
  `design.md` §3.1's CONFIRMED visual decision states that a sold-out product (`stock === 0`)
  should render the UI Kit's sold-out **badge-overlay pattern** (62% opacity + "품절" tag applied
  to the product image/card) *instead of* the size grid, specifically so the "all-disabled grid"
  and the mockup's "some sizes disabled" visual languages never appear together. The shipped
  implementation always renders the full size grid (all 10 buttons, uniformly `disabled` when
  `stock === 0`) plus a plain text "품절" label next to the stock count — no overlay pattern is
  applied to the image, and the grid is never suppressed. This does **not** violate REQ-BRAND-022,
  REQ-BRAND-023, or AC-BRAND-023 as literally written (all size options are indeed uniformly
  disabled), and no test asserts the overlay pattern, so it is not a regression against any
  binding acceptance criterion — but it is a documented `design.md` decision that shipped code
  does not implement, and this drift is not mentioned anywhere in `progress.md`'s correction-cycle
  entry or PROVISIONAL list. Required fix (non-blocking): either implement the confirmed overlay
  pattern on `ProductGallery`/`ProductDetailView` for the sold-out case, or amend `design.md` §3.1
  to describe the "always show the disabled grid" behavior that actually shipped, so the design
  artifact does not misrepresent the shipped UI for a future reader.

- **F4** [Low] [optional] `src/app/globals.css:82-84` (`.plate` class) — REQ-BRAND-009 required
  the stylesheet to "provide" a `.plate` class with `grayscale(1) contrast(1.05)`, and it does
  (AC-BRAND-010 passes literally). However, `grep -rn "plate" src --include="*.tsx" --include="*.ts"`
  returns zero matches — the class is never applied to any image wrapper in the codebase
  (`ProductGallery.tsx`, `BrandLogo.tsx`, `ProductCard.tsx` all render plain `<Image>` with no
  `.plate` class). This is dead CSS as of this commit. Required fix (non-blocking): apply `.plate`
  to at least the product-gallery image wrapper (the component `research.md` §7 names as the
  intended `.plate` consumer), or note explicitly in `design.md`/`progress.md` that this milestone
  only provisions the class for a later consumer.

- **F5** [Info] [non-blocking, procedural] — `CHANGELOG.md` carries no entry for SPEC-BRAND-001
  (`grep -n "SPEC-BRAND-001" CHANGELOG.md` → no output), `spec.md` frontmatter `status:` is still
  `in-progress` (not `completed`), and `progress.md` §E.4 (Sync-phase Audit-Ready Signal) is still
  the placeholder `_<pending sync-phase>_`. This indicates `manager-docs` has not yet produced the
  sync-phase deliverables (CHANGELOG entry, docs update, the `implemented → completed` status
  transition) for this SPEC. This is not a code-quality defect and is outside this audit's scoring
  dimensions, but the orchestrator should ensure `manager-docs` completes the sync commit before
  treating SPEC-BRAND-001 as closed — a sync-auditor PASS verdict on the implementation does not
  substitute for the sync-phase documentation step.

- **F6** [Info] [non-blocking, reminder per dispatch instruction] — Per user memory
  (`feedback_sync-auditor-self-persist-report.md`), multiple prior SPECs in this session forgot to
  commit their sync-audit report to git. This report has been written to disk at
  `.moai/reports/sync-audit/SPEC-BRAND-001-2026-09-07.md` as instructed, but **committing it is
  not this agent's responsibility** (no Write-to-commit tool available here). Whoever picks up
  sync-phase docs next should confirm this file (and the other currently-untracked sync-audit
  reports visible in `git status` — `SPEC-ADMIN-002`, `SPEC-ADMIN-003` ×2, `SPEC-REVIEW-001`) gets
  committed, not left as an untracked local artifact.

## Recommendations

1. Fix F1 and F2 together in one small commit (both are stale-comment corrections of the same
   shape) — cite `design.md` §2.3 and §2.5 respectively in the corrected comments.
2. Decide F3 explicitly: either implement the confirmed overlay pattern, or amend `design.md`
   §3.1 to match shipped behavior. Either resolution is acceptable; leaving the drift undocumented
   is the only wrong answer.
3. F4 is the lowest priority of the four code findings — safe to defer to a later SPEC that
   actually needs `.plate` (e.g., when real product photography replaces the picsum placeholders).
4. Route F5 to the orchestrator: dispatch `manager-docs` for the sync-phase CHANGELOG + status
   transition before considering SPEC-BRAND-001 closed.
5. The 2 legitimate PROVISIONAL carryover items (product image paths, category-slug romanization)
   and the internally-self-contradictory "약 4주" production lead-time value are correctly treated
   as non-blocking per the dispatch's explicit instruction — this audit does not re-litigate them.
