# .moai/project/harness-spec.yaml — machine-readable harness generation input
# Generated by /moai project from .moai/project/interview.md — do not hand-edit;
# re-run /moai project to regenerate from updated interview answers.

# --- REQUIRED base fields (Stage A) ---
domain: e-commerce-web
goal: >-
  Let customers search and browse products, add them to a cart, and complete
  checkout (including guest checkout) on a mobile-first B2C fashion web app.
constraints:
  - payment-data consistency is the top priority
  - catalog response p95 <= 300ms
  - minimize personal-data collection (PII minimization)
scope: >-
  In-scope: product catalog/search, cart, payment-gateway (PG) checkout
  integration, order/shipping-status tracking, product reviews, admin
  product/order management, guest checkout, mobile-first responsive UI.
  Out-of-scope (for now): external shipping-carrier tracking API integration,
  multi-vendor/marketplace features.

# --- EXTENDED fields (Stage B) ---
verification: "automated tests (Jest/Vitest-style)"
external_systems:
  - payment gateway (PG) provider
  - database
ui_surface: has-ui
team_sharing: solo
